version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: epicentra
      POSTGRES_PASSWORD: epicentra
      POSTGRES_DB: epicentra
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
